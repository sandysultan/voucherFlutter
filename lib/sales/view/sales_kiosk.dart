import 'package:flutter/material.dart';

class SalesKiosk extends StatelessWidget{
  const SalesKiosk({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text(''),),body: Placeholder(),);
  }

}